{
  "name": "@v2icons/utils",
  "version": "0.1.1",
  "dependencies": {
    "css-render": "^0.12.0"
  },
  "module": "es/index.js",
  "main": "lib/index.js",
  "author": "07akioni",
  "license": "MIT",
  "files": [
    "es",
    "lib"
  ],
  "repository": {
    "url": "https://github.com/07akioni/xicons"
  },
  "scripts": {
    "build": "cp ../style.ts src && rm -rf es lib && tsc -p tsconfig.cjs.json && tsc -p tsconfig.esm.json",
    "release": "npm run build && npm publish --access public",
    "dev": "cp ../style.ts src && vite"
  },
  "devDependencies": {
    "vite": "^2.0.1",
    "vue": "^2.6.12"
  }
}
