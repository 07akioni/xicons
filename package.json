{
  "name": "xicons-workspace",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap && lerna run init --scope=xicons --stream && lerna run build:for-site --scope=xicons --stream",
    "build:vercel": "lerna run build --scope=xicons-site --stream"
  },
  "author": "07akioni",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.21.0",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1"
  },
  "repository": {
    "url": "https://github.com/07akioni/xicons"
  }
}
